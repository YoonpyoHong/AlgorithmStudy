#include <iostream>
#include <stack>
using namespace std;

stack<int> result;

int main(){
    // result.pop();
    if(result.empty()){
        cout << "check";
    }
    if(!result.empty()){
        cout << "check2";
    }
}